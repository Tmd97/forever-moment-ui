// Admin feature store configuration
// This file will export the admin-specific reducer and any middleware

export const adminReducer = (state = {}, action: any) => {
    switch (action.type) {
        default:
            return state;
    }
};
