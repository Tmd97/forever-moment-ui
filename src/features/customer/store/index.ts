// Customer feature store configuration
// This file will export the customer-specific reducer and any middleware

export const customerReducer = (state = {}, action: any) => {
    switch (action.type) {
        default:
            return state;
    }
};
